interface Window {
  /**
   * Environment variable to configure the action stream sampler delay in milliseconds
   * Used in workbench.ts to control the rate of streaming updates
   */
  ENV_SAMPLER_DELAY?: string;
}
